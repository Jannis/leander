overwrite: true
schema: "http://localhost:3000/github/graphql"
documents: null
generates:
  src/generated/graphql/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
